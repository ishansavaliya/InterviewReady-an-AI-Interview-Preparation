import * as React from "react";

import { cn } from "@/lib/utils";

// Input component with forwarded ref
// A styled HTML input element with consistent design language
const Input = React.forwardRef<HTMLInputElement, React.ComponentProps<"input">>(
  ({ className, type, ...props }, ref) => {
    return (
      <input
        type={type}
        className={cn(
          // Base styling for all inputs with comprehensive states:
          // - Normal, focused, disabled states
          // - Border, padding, and text styling
          // - Special handling for file inputs
          // - Responsive text size (base on mobile, smaller on desktop)
          "flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-base ring-offset-background file:border-0 file:bg-transparent file:text-sm file:font-medium file:text-foreground placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50 md:text-sm",
          className
        )}
        ref={ref}
        {...props}
      />
    );
  }
);
Input.displayName = "Input";

export { Input };
